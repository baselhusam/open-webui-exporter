OPENWEBUI_BASE_URL=http://localhost:3000
OPENWEBUI_API_KEY=changeme
POLL_INTERVAL_SECONDS=30
EXPORTER_PORT=9090

# Optional $/1k-token rates for the "Estimated Spend" cost metric/panel.
# Leave at 0 for local models (Ollama); set these when routing to paid
# providers (e.g. OpenAI) to estimate spend from per-user token counts.
COST_PER_1K_INPUT_TOKENS=0
COST_PER_1K_OUTPUT_TOKENS=0
